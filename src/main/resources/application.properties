spring.application.name=Laboratory_Booking_System
spring.datasource.url=jdbc:mysql://localhost:3306/labbookingdb?createDatabaseIfNotExist=true
spring.datasource.username= root
spring.datasource.password= root
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.datasource.driver-class-name= com.mysql.cj.jdbc.Driver

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql=true
#server.servlet.context-path=/api

#Exclude spring security autoconfiguration. The following option has already been set in @SpringBootApplication() annotation.
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.SecurityAutoConfiguration

# Session timeout in seconds (30 minutes)
server.servlet.session.timeout=1800

